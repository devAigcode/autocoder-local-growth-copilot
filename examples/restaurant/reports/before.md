# Juniper Brick — Before report

- Source: https://juniperbrick.autocoder.store/
- Audit mode: Browser-rendered
- Tool version: 0.1.1
- Evidence coverage: 100% in all categories

## Scores

| Category | Score |
|---|---:|
| Discoverability | 50 / 100 |
| Local relevance | 75 / 100 |
| Conversion readiness | 65 / 100 |

## Top actions

### 1. Add a clear booking action

No interactive label matched the expected booking phrases. Add one prominent “Book a Table” action that leads to a practical reservation path.

### 2. Improve the homepage title

The title is `Juniper Brick`, which does not explain the restaurant category or location. Use a concise title such as `Farm-to-Table Restaurant in Buffalo | Juniper Brick`.

### 3. Add Restaurant structured data

No JSON-LD schema was detected. Add an accurate `Restaurant` object that matches visible business information.

## Additional failed checks

- No meta description was found.
- No canonical URL was found.
- `/robots.txt` returned the HTML application shell instead of crawler directives.
- `/sitemap.xml` returned the HTML application shell instead of an XML sitemap.

## Notes

The live site is client-rendered. This baseline was captured with browser mode enabled. Automated tests use normalized local fixtures rather than the live URL, so the expected result remains deterministic.
